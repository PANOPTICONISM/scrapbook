# Copy this file to `.env` (next to docker-compose.yml) and set a strong token.
# This is the API token the Flutter app must send to sync with the server.
#
# Generate a good one with:
#   openssl rand -hex 32
SCRAPBOOK_TOKEN=replace-with-a-long-random-secret

# Port the server listens on.
PORT=47291
